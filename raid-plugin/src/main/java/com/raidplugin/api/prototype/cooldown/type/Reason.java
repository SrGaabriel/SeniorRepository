package com.raidplugin.api.prototype.cooldown.type;

/**
 * That's a reason from cooldown.
 * The enum identify that have the cooldown.
 */
public enum Reason {
    ATTACKED,
    DEFENDED,
    OTHER
}
